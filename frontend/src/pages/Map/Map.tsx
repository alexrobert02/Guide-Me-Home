import * as React from "react";
import MapWrapper from "../../components/MapWrapper";

const Map = () => {
    return (
        <div>
            <MapWrapper />
        </div>
    );
};

export default Map;